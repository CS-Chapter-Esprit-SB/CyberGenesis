[project]
name = "client-youssefzou"
version = "0.1.0"
description = "Client setup package"
readme = "README.md"
authors = [
    { name = "youssef", email = "zouaghiyousse@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pre-commit>=4.6.1",
    "pytest>=9.1.1",
]

[project.scripts]
client-youssefzou = "client_youssefzou:main"

[build-system]
requires = ["uv_build>=0.11.21,<0.12.4"]
build-backend = "uv_build"
