[project]
name = "client-by-latifa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "benyahiia", email = "benyahia.latifa@esprit.tn" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
client-by-latifa = "client_by_latifa:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
