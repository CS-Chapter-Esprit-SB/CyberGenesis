[project]
name = "server-by-url-shortener"
version = "0.1.0"
description = "URL Shortener Core Service: CRUD API for short URLs backed by PostgreSQL"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.30.0",
    "fastapi>=0.115.0",
    "pydantic-settings>=2.4.0",
    "sqlmodel>=0.0.22",
    "uvicorn[standard]>=0.30.0",
]

[dependency-groups]
dev = [
    "aiosqlite>=0.20.0",
    "httpx>=0.27.0",
    "pre-commit>=4.6.1",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
]

[project.scripts]
server-by-url-shortener = "server_by_url_shortener.main:run"

[build-system]
requires = ["uv_build>=0.11.21,<0.12.0"]
build-backend = "uv_build"
